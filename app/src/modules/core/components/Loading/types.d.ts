export interface LoadingProps {
  /**
   * The margin surrounding the loader. Will be
   * passed as the CSS `margin` property.
   */
  margin?: string;

  /**
   * The size of the component. Will be passed as
   * the CSS `height` and `width` properties.
   */
  size?: string;
}
