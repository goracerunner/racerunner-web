export interface PropertyProps {
  /**
   * The title to show above the property.
   */
  title: string;

  /**
   * If this is `true`, the default bottom margin will be removed.
   */
  noMargin?: boolean;
}
