export interface HeaderProps {
  /**
   * If `true`, the colors used will be inverted
   * to suite a light background.
   * @default false
   */
  inverted?: boolean;

  /**
   * If `true`, the header will be smaller.
   */
  reduced?: boolean;
}
